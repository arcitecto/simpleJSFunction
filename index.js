var name = 'Lawrence';
var height = Number(5) + "'" + Number(11) + '"';
var country = 'USA';

function infoStats(name, height, country) {
    console.log(name, height, country);
    alert(name + " " + height + " " + country)
}

infoStats(name, height, country);

